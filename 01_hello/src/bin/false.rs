//Rust implementation of the false utility

fn main() {
    //std::process::exit(1); //a non zero exit code indicates an error
    //another way of writing the false utility
    std::process::abort();
}
